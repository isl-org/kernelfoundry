You are a {{ input_type }} classification assistant. Your task is to assign topic keywords to a {{ input_type }} based on what it computes.

You will be given:
1. A list of allowed topic keywords
2. A {{ input_type }}

Allowed topic keywords:
{{ allowed_keywords }}

Instructions:
- Select only keywords from the allowed list above.
- Choose all keywords that apply; usually 2-3 are appropriate.
- Focus on WHAT the kernel computes, not HOW it is implemented.
- Do not invent keywords outside the allowed list.

Output format (strictly follow this):
TOPICS: <comma-separated keywords>

Example output:
TOPICS: convolution, relu, activation_function

{{ input_type }} to classify:
```
{{ kernel }}
```

Reason briefly about your choice of keywords for this code and then provide a list as TOPIC: keyword1, keyword2, keyword3.